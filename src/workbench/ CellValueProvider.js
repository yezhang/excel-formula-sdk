/**
 * 可以访问表格中的全部数据，包括所有 Sheet 下的全部单元格数据。
 */
class CellValueProvider {
  constructor() {
    this.workingSheet = null;
  }

  /**
   * 公式引擎识别后的单元格地址字面量
   */
  getCellValue(cellAddressString) {
    
  }
}